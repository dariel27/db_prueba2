<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itla.db_prueba2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MateriaRow"></activity>
        <activity android:name=".row_carrera" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity_crear_carrera" />
        <activity android:name=".activity_inicio_estudiante" />
        <activity android:name=".activity_materia" />
        <activity android:name=".activity_estudiante" />
        <activity android:name=".activity_inicio_carrera">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>